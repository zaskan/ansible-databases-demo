---
- name: Deploy Azure Virtual Machine
  hosts: localhost
  gather_facts: false
  tasks:

    - name: Deploy Azure Network
      infrastructure.provision.azure_infra:
        name: 

    - name: Deploy Azure Visrtual Machine
      infrastructure.provision.azure_vm:
        name: 